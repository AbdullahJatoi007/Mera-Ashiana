import 'package:flutter/material.dart';

class AppDarkColors {
  // Primary / Brand Colors
  static const Color primaryNavy = Color(0xFF102A4C); // Slightly lighter navy
  static const Color accentYellow = Color(0xFFFFD54F); // Softer yellow

  // Neutral Colors
  static const Color background = Color(0xFF121212);
  static const Color surface = Color(0xFF1E1E1E);

  // Text & UI Colors
  static const Color textDark = Colors.white;          // Main text color
  static const Color textPrimary = Colors.white;       // For bold headings or important text
  static const Color textSecondary = Colors.white70;   // Secondary text / hint text
  static const Color iconColor = Colors.white;         // For icons / reset / search icon
  static const Color borderGrey = Color(0xFF2C2C2C);  // Borders / dividers
  static const Color errorRed = Color(0xFFEF5350);     // Error messages
}